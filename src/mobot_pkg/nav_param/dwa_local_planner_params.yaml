DWAPlannerROS:

# Robot Configuration Parameters
  max_vel_x: 0.14  #0.3  # 0.5
  min_vel_x: 0.07  # 0.0

  max_vel_y: 0.14  # diff drive robot
  min_vel_y: 0.07 # diff drive robot

  max_trans_vel: 0.14
  min_trans_vel: 0.07
  trans_stopped_vel: 0.3

  max_rot_vel: .8  # 1.0
  min_rot_vel: 0.5  # could set this lower
  rot_stopped_vel: 0.3
  
  controller_frequency: 5
  acc_lim_x: .7
  acc_lim_theta: .7 # 1.0 
  acc_lim_y: .7      # diff drive robot

# Goal Tolerance Parameters
  yaw_goal_tolerance: 0.05  # 0.05
  xy_goal_tolerance: 0.1  # 0.10
  # latch_xy_goal_tolerance: false

# Forward Simulation Parameters
  sim_time: 1.5       # 1.7
  vx_samples: 10       # 3
  vy_samples: 2      # diff drive robot
  vtheta_samples: 5  # 20

# Trajectory Scoring Parameters
  path_distance_bias: 48.0      # 32.0   - weighting for how much it should stick to the global path plan
  goal_distance_bias: 64.0      # 24.0   - wighting for how much it should attempt to reach its goal
  occdist_scale: 0.1           # 0.01   - weighting for how much the controller should avoid obstacles
  forward_point_distance: 0.325 # 0.325  - how far along to place an additional scoring point
  stop_time_buffer: 0.1         # 0.2    - amount of time a robot must stop in before colliding for a valid traj.
  scaling_speed: 0.25           # 0.25   - absolute velocity at which to start scaling the robot's footprint
  max_scaling_factor: 0.15       # 0.2    - how much to scale the robot's footprint when at speed.

# Oscillation Prevention Parameters
  oscillation_reset_dist: 0.05  # 0.05   - how far to travel before resetting oscillation flags

# Debugging
  publish_traj_pc : true
  publish_cost_grid_pc: true
  global_frame_id: odom


# Differential-drive robot configuration - necessary?
#  holonomic_robot: false